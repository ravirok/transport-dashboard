# Transport Risk Agent — Python dependencies

# Core LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# FastAPI server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0            # async HTTP for future real-data tool calls

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx                    # TestClient for FastAPI

# Optional — for richer dev experience
rich>=13.0.0             # pretty terminal output when running the CLI

{
  "name": "transport-dashboard-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve . -l 8080",
    "build": "esbuild src/index.js --bundle --outfile=bundle.js --minify"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "axios": "^1.5.0",
    "recharts": "^2.7.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.2",
    "serve": "^14.2.0"
  }
}

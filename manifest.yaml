applications:
  - name: your-app-name
    memory: 256M
    buildpacks:
      - nodejs_buildpack
    services:
      - Test-demo        # ← your xsuaa instance name in BTP
      - BTP-DEMO  # ← your destination instance name in BTP
    env:
      DESTINATION_NAME: S48  # ← exact name from BTP Cockpit

ID: transport_dashboard_mta
_schema-version: "3.3"
version: 1.0.0

modules:

  - name: transport-dashboard-backend
    type: nodejs
    path: .
    parameters:
      memory: 1024M
      disk-quota: 1024M
      buildpack: nodejs_buildpack
    provides:
      - name: backend-service
        properties:
          url: ${default-url}

  - name: transport-dashboard-frontend
    type: html5
    path: .
    parameters:
      memory: 512M
      disk-quota: 512M
      url: /
      authentication: none
    requires:
      - name: backend-service
        properties:
          url: "https://hcl-america-solutions-inc--hclbuild-g03o2ijo-dev-transp28ffac8d.cfapps.eu10-004.hana.ondemand.com"

ID: transport_dashboard_mta
_schema-version: "3.3"
version: 1.0.0

modules:

  - name: transport-dashboard-backend
    type: nodejs
    path: backend
    parameters:
      memory: 1024M      # Required for Node.js staging
      disk-quota: 1024M
      buildpack: nodejs_buildpack
    provides: []
    requires: []

  - name: transport-dashboard-frontend
    type: nodejs
    path: frontend
    parameters:
      memory: 1024M      # Required for Vite + React build
      disk-quota: 1024M
      buildpack: nodejs_buildpack
    provides: []
    requires: []

resources: []
